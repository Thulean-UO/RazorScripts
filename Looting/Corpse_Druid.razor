// This script uses command
// '.rip' so it works only
// when morphed.

if findtype 8198 true as 'corpse'
    target 'corpse'
    say '.rip'
else
    hotkey 'Target Self'
    sysmsg 'No Corpse!' 33
endif
